import express from 'express';
// import controller associated with the route here
// import model associated with the route here

const router = express.Router();

//rotes (get, post, put, delete)

export default router;
